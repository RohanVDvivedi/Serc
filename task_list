 * remove dstring's state_level dependency on http request and response parsers
 * use cashed table for caching files instead of dmap
 * dmap to hold dstring value inside of it, ad use it explicitly for headers and parameters only